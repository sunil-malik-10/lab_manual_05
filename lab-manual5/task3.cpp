#include <iostream>
#include <cmath>
using namespace std;
main()
{
    float number,result;
    cout<<"Enter a number:";
    cin>>number;
    result=sqrt(number);
    cout<<"The square root of "<<number<<" is : "<<result;
    return result;
}
